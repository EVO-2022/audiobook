# Caddyfile for public domain with Let's Encrypt
# IMPORTANT: Replace YOUR_DOMAIN_HERE with your actual domain (e.g., mom.example.com)
# This file is used by docker-compose.prod.yml
# The prod-up.sh script will attempt to replace YOUR_DOMAIN_HERE with the DOMAIN from .env

YOUR_DOMAIN_HERE {
    reverse_proxy audiobookshelf:80

    # Optional: Add security headers
    header {
        # Enable HSTS
        Strict-Transport-Security "max-age=31536000;"
        # Prevent clickjacking
        X-Frame-Options "SAMEORIGIN"
        # XSS protection
        X-Content-Type-Options "nosniff"
    }

    # Optional: Logging
    log {
        output file /var/log/caddy/access.log
    }
}

